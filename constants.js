const USERS = {
    PHIL: "PHIL",
    TOM: "TOM",
};

const STATES = {
    LONG: "LONG",
    SHORT: "SHORT",
};

const Config = {
    DEFAULT_STATE: STATES.LONG,
    BUYIN: 50
}

module.exports = { USERS, STATES }